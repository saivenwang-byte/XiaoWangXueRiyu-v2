---
description: 铁律 · 每次改 UI 须在真机尺寸小程序框中目视最终态；改码即刷（保存刷新一次）
alwaysApply: true
---

# 真机尺寸小程序预览 · 铁律（与讨论优先并列）

> 真源：[docs/学员端双界面显示标准-首要执行.md](../../docs/学员端双界面显示标准-首要执行.md) · [docs/双通道验收-浏览器与手机真机框.md](../../docs/双通道验收-浏览器与手机真机框.md)

## 最终成果 · 两种显示界面

1. **本地 Cursor 预览器**：`cursor-miniapp-phone.html?live=1`（§二 锁定显示）  
2. **可转发微信链接**：`index.html?v=CACHE_VER` 公网 https（`发布前自检` 后）

## 铁律（用户拍板 · 每一次改 UI / 刷星 / 修改）

1. **最终呈现**必须以 **真实手机逻辑尺寸（390×844 壳 · web-view 753px 高）** 内、展示 **小程序 WebView 真实使用界面** 为准；不得以桌面全宽、未套壳 iframe 或口头描述代替。
2. **在壳内操作各 UI**；切换课文/笔记/注音/课内/五关时，**外壳 390×844 尺寸不变**（仅壳内内容滚动/换页）。
3. **每一次**刷星、改样式、改课内/マイ/笔记/入門 后，交付前须在真机框内看到 **本轮最新保存** 的界面。
4. **Cursor 开发**：Simple Browser 打开 **`cursor-miniapp-phone.html?live=1`**；`local-preview-server.py --watch` 监听保存后 **刷新 web-view 一次**；Agent 收尾 `python scripts/preview-notify-reload.py`。

## Agent 强制动作

| 时机 | 动作 |
|------|------|
| **改 UI 前** | 确认 `8765` 已起；给出真机预览链接（含 `?live=1`） |
| **改 UI 后** | 不得声称修好；须写明「请在真机框目视 / 已目视」 |
| **交付用户** | 交付反馈块含：真机预览 URL、`miniappPreview=1`、双通道目视行 |

## 唯一入口（按优先级）

1. **Cursor / 改码即刷**：`http://127.0.0.1:8765/cursor-miniapp-phone.html?live=1`（须 `重启本地服务.bat`）
2. 双击 **`打开双通道预览.bat`**（浏览器 A + 真机框 B，B 带 live）
3. **`cursor-预览-点这个.html`** → 真机尺寸按钮

## 禁止

- 只改仓库、不打开真机框就交付
- 仅用 Cursor 全宽编辑区想象最终效果
- 用公网链接在本机代替改码轮次的目视验收
