# 学员端双界面显示标准 · 首要执行与 UI 设计规范补充

> **地位**：学员端 UI 的**首要执行标准**；对 [DESIGN-L0-图标与人设全景.md](./DESIGN-L0-图标与人设全景.md)、[学员端显示界面要求-会议纪要汇编.md](./学员端显示界面要求-会议纪要汇编.md)、[PRODUCT-PM-全局导演手册.md](./PRODUCT-PM-全局导演手册.md) 的**补充条款**（冲突时以本文 **§二 Cursor 锁定显示** 与 **§一 双界面** 为交付判据）。  
> **用户拍板**：2026-05-26  
> **Cursor 规则**：`.cursor/rules/miniapp-real-device-preview-iron-law.mdc` · `.cursor/rules/ui-display-reread-before-preview.mdc`

---

## 一、最终成果的两种显示界面

标日 H5 学员端**交付完成**时，必须同时满足以下两种显示界面（缺一不可）：

| # | 界面 | 用途 | 唯一正确入口 | 通过标准 |
|---|------|------|----------------|----------|
| **1** | **本地 Cursor 预览器** | 开发、改 UI、交付前目视验收 | `http://127.0.0.1:8765/cursor-miniapp-phone.html?live=1`（一键：`Cursor真机持续预览.bat` / `打开双通道预览.bat`） | 在 **§二** 锁定尺寸壳内操作、切换各页仍符合 §二 |
| **2** | **可转发微信链接** | 学员真机学习、人传人转发 | `https://saivenwang-byte.github.io/XiaoWangXueRiyu-v2/index.html?v=410`（固定公开入口） | 微信内打开与本地真机框 **同一套最新 H5**；公开链接不随内部缓存版本变化 |

**说明**

- 界面 **1** 不是桌面全宽浏览器代替手机；须走 **390×844 真机逻辑壳**（见 §二）。
- 界面 **2** 不是 `localhost` / `file://` / 同 WiFi IP；见 `.cursor/rules/production-netlify.mdc`。
- 日常开发可另开 **通道 A**（全宽 `index.html?v=`）对照数据与链接，但**不得**用 A 单独声称「最终 UI 已验收」——终检须 **1 + 2**（详见 [双通道验收-浏览器与手机真机框.md](./双通道验收-浏览器与手机真机框.md)）。

---

## 二、Cursor 浏览器 · 锁定显示（铁律 · UI 设计规范补充）

在 **Cursor 内置浏览器 / Simple Browser** 中验收界面 **1** 时，必须满足：

| # | 要求 | 含义 | 真源 / 实现 |
|---|------|------|-------------|
| **L1** | **真实手机屏幕尺寸** | 外壳固定 **390×844** 逻辑像素（iPhone 14 / 微信开发者工具同规格）；内容区 **753×390**（扣状态栏 47px + 导航条 44px） | `cursor-miniapp-phone.html` `:root --phone-w` / `--phone-h` |
| **L2** | **在真实手机屏幕中操作各 UI** | 在壳内 iframe（`miniappPreview=1`）中点按、滚动、切换底栏 Tab、进课、五关、笔记、入門；体验与微信 WebView **同一视口与触摸区** | H5 加载 `css/miniapp-webview.css`；`index.html?…&miniappPreview=1` |
| **L3** | **切换界面时尺寸不变** | 从开机 / 地图 / 课内 / 笔记 / 注音（`intro.html`）任意切换，**外壳宽高不得随页面变化**；仅 **壳内 web-view 内容** 滚动或换页 | 禁止把 Simple Browser 拉成全屏当验收；禁止 iframe 随内容撑高壳体 |

### L3 验收动作（每次改 UI 后建议走一遍）

1. 打开 `cursor-miniapp-phone.html?live=1`，确认外框仍为 **390×844**。
2. 壳内依次：**课文（地图）→ 笔记 → 注音 → 进某一课 → 五关 Tab 切换 → 返回地图**。
3. 全程：**外框像素尺寸不变**；底栏、顶栏比例与首屏一致；无整页横向溢出。

### Cursor 推荐操作

```text
Ctrl+Shift+P → Simple Browser: Show
→ 粘贴：http://127.0.0.1:8765/cursor-miniapp-phone.html?live=1
→ 侧边固定，勿拉成全屏替代真机框
```

须先 **`重启本地服务.bat`**（`local-preview-server.py --watch`）；保存 `css/` / `js/` / `html` 后 web-view **刷新一次**（`?live=1`）。

---

## 三、界面 2 · 微信可转发链接（补充条款）

| 项 | 要求 |
|----|------|
| **链接形态** | `https` 公网；固定 `?v=410` 兼容入口 |
| **与界面 1 一致** | 同一仓库；公开 `PUBLIC_LINK_VER=410`，内部资源使用当前 `CACHE_VER`；push 后 Pages 更新 |
| **作者发链** | `帮你发布好了.bat` 或首页 📤 / `share.html` |
| **交付前** | `发布前自检.bat` 全 OK 后，方可在微信抽样界面 2 |

---

## 四、与其它文档的关系

| 文档 | 关系 |
|------|------|
| [学员端显示界面要求-会议纪要汇编.md](./学员端显示界面要求-会议纪要汇编.md) | 控件级、折叠、底栏、笔记等 **目视清单**（§0 R1–R11）；刷新前必读 |
| [双通道验收-浏览器与手机真机框.md](./双通道验收-浏览器与手机真机框.md) | 开发期 **A+B 双通道** 纪律；B = 本文界面 1 |
| [Agent交付前工作流.md](./Agent交付前工作流.md) §C2 | 改码前 / 交付前双通道与交付块 |
| [DESIGN-L0-图标与人设全景.md](./DESIGN-L0-图标与人设全景.md) | 图标、高度预算、L0 导航；本文不替代 L0 |
| [发布前自检.md](./发布前自检.md) | 机器闸门；通过后做界面 1 + 2 目视 |

---

## 五、Agent / 协作者执行清单

**改 UI 前**

- [ ] `8765` 已起（`--probe` OK）
- [ ] 界面 **1** 已打开且为 **390×844 壳**（非全宽 Simple Browser）
- [ ] 已读 [学员端显示界面要求-会议纪要汇编.md](./学员端显示界面要求-会议纪要汇编.md) §0

**改 UI 后 · 声称修好前**

- [ ] 界面 **1**：§二 L1–L3 走通（含切换底栏/进课）
- [ ] 界面 **2**：地址保持 `?v=410`，内部资源与 `CACHE_VER` 一致；`发布前自检` 全 OK
- [ ] 交付块写明：双界面已目视 / 微信链 `v=__`

**禁止**

- 仅用 Cursor 全宽预览代替界面 1
- 验收时改变真机壳尺寸或去掉壳直接打开 `index.html` 全屏
- 未跑自检就向用户提供「最终版」公网链

---

## 六、修订记录

| 日期 | 内容 |
|------|------|
| 2026-05-26 | 用户拍板：最终双界面（Cursor 真机壳 + 微信链）；§二 L1–L3 Cursor 锁定显示 |
