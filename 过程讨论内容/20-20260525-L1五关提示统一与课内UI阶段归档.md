# 阶段归档 · 2026-05-25 · 第1課课内 UI 与首页 IA

> **状态**：用户确认收尾，已写入 `iteration-baseline.json` · cache **v=225**  
> **范围**：第1单元第1課 MVP 课内 + 首页/开屏 IA（不扩 24 课批量）

---

## 一、第1課五关 · 提示统一（核心）

### 决策

| 项 | 定案 |
|----|------|
| **打开方式** | 五关（単語/会話/文法/作業/拡張）统一：**「提示：」+ ▾**，默认收起，点标题展开 |
| **L1 单行** | 不再单行静态铺满；`SenseiTipCard` 增加 `l1Scope`，第1課强制走 `hyouga-tip-fold` |
| **槽位** | 统一容器 `.l1-tip-slot`（淡黄底 + 左橙条仍用 `.hyouga-tip-shell`） |
| **单词注意/延伸** | 去掉「注意·要点 / 延伸·N条」自定义块，并入同一套折叠提示；**活用**仍为数据区 |

### 实现文件

- `js/sensei-tip-card.js` — `l1Scope` / `alwaysFold`
- `js/l1-knowledge-card.js` — 第1課委托时传 `l1Scope: true`
- `js/dialogue-gate.js` — ABC/场景/行内提示；去掉 `hyouga-tip-static--inline`
- `js/lesson-1-flow.js` — 单词展开区 `vocabTipSlotHtml`；文法/作业/扩展 `l1-tip-slot--gate`
- `css/mvp.css` — `.l1-tip-slot*`、单词行 grid、序号 26px 栅格

### 验收

1. 第1課五关各有一处「提示：」，交互一致（点开展开）。  
2. 单词第 1 项序号与文法/作业关 `--l1-seq-size` 视觉对齐。  
3. 强刷 `index.html?v=225`。

---

## 二、同阶段已并入的其他 UI（摘要）

| 主题 | 要点 | 主要文件 |
|------|------|----------|
| 五关当前态 | 方案 A：左侧色条桥接 Tab↔内容区 | `css/mvp.css` · `docs/产品设计-五关当前态轻量高亮-方案.md` |
| L3 听录播色 | 略深于中文灰，话筒可用性文案 | `js/hyouga-glyphs.js` · `js/shadow-speak.js` |
| 入門 CI | 顶栏紫色系对齐 Part0 | `intro.html` · `css/mvp.css` |
| 入門完成 | 全完成回课文首页 | `js/intro-guide.js` |
| 首页 IA | 开屏地图；课表去地图条；单元星/主题/彩蛋最右 | `js/home-splash.js` · `js/journey-home.js` · `assets/splash/japan-map-splash.png` |

---

## 三、版本与链接

| 项 | 值 |
|----|-----|
| 产品版 | 1.0.2（第1課 MVP 冻结不变） |
| 缓存 build | **225** |
| 本地 | `http://localhost:8765/index.html?v=225` |
| 公网 | `https://saivenwang-byte.github.io/XiaoWangXueRiyu/index.html?v=225`（push 后生效） |

`js/share-wechat.js` 的 `CACHE_VER` 与 `index.html` 资源 `?v=` 须同步。

---

## 四、机器可读索引

- `docs/iteration-baseline.json` → `confirmed[]` 条目 `l1-tip-unified-v225`
- `docs/version-history.json` → `releases[]` cache 225
- `docs/Agent交付前工作流-记忆库.md` → §L1 提示统一

---

## 五、未纳入本 commit 的邻域（仍本地未跟踪）

- 大量 `tts-cache/*.mp3` 增量、漫画分镜 docs、`mock/`、OpenClaw 壳 — 需单独发版决策后再 push。
